apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: client-info-secret
  namespace: synology-csi
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: parameterstore
    kind: ClusterSecretStore
  target:
    name: client-info-secret
    creationPolicy: Owner
  data:
  - secretKey: client-info.yml
    remoteRef:
      key: /homelab/prod-synology-csi-secret
